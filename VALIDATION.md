# 2026-09-08 本机验收

## 原生产品操作与设置（本轮）

- 安装版实际操作通过：单击选择 → Space 打开原图 → Right 切换文件与图像 → Space 关闭；⇧Right 扩选，⌘⌫ 进入精确两项预检与确认。
- 删除确认窗口出现外部操作；CUA 取消请求被 `The user changed ...` 拒绝，随后 App 回读显示两张已删除。已停止删除测试并向用户确认来源，未将这次操作当作无人值守删除验收通过。此前合成测试照片的可恢复删除证据仍见下节。
- ⌘F 聚焦搜索，输入空格保留在文本框，⌘A 仅选中文字；没有打开预览或勾选全部照片。
- 暂停/继续自动整理真实切换通过；进入重复核对显示 22 项，其中仅符合保护规则的 1 项自动勾选，保留副本及其余待核对项未勾选。
- ⌘, 打开真实 Settings 窗口；照片大小 180 → 220，关闭再打开仍为 220，随后恢复 180。通用、整理规则、快捷键页面布局实查通过；快捷键全部默认未设置，点击录制后 Esc 可取消。未改用户登录项，未注册用户全局测试键。
- 视频实测发现 macOS 27 beta 的 `_AVKit_SwiftUI` 泛型元数据初始化崩溃；改用 `AVPlayerView` 后冷启动，同一 7 秒视频成功渲染/播放、切回照片并用空格关闭。第二次故障来自安装完成前已重新打开的旧进程；必须冷启动复验。
- 新时间线视觉验收已解除上轮连接阻塞：个人/猫/会议工作筛选、片段展开及真实封面显示通过，窗口宽度被照片网格使用。实际显示 101 个相册、6,322 项、2,677 个片段；全库已完成识别尝试，内容缺失 13 项、失败 0，不把缺失内容称为已理解。
- 隔离 UserDefaults 的真实 Swift 模型/快捷键测试通过：单选、⌘增减、⇧范围、只读边界、Space/Esc/方向键、跨 60 项分页、文本输入隔离、设置持久化、空默认、局部/全局作用域、冲突、注册失败保留原值、录制暂停/恢复及清除。注册器故障由测试替身注入。
- 25 条 Python 回归通过；旧六种真实计划与时间线经实际 Swift decoder 通过。冻结引擎独立数据目录验证设置：包含共享 6,322 项、仅个人 4,616 项，成员数守恒；禁用内容识别后 `journey-enrich` 不执行识别。所有诊断只写隔离本地索引。

## 自动时间线改造

- 全部 101 个相册、6,324 项照片与视频参与本地时间线归集；其中 1,706 项共享内容只读。对实际输出验证每个资产恰好进入一个事件片段，事件总数与照片总数守恒，人物/猫/会议等视图是同一索引的筛选。
- 已安装 App 启动后自动启动整理进程，真实缓存从 14 项持续增加至 1,334 项，阶段检查内容识别失败 0。退出重启后继续使用缓存。首次全库内容识别仍需后台运行，不能称全部语义识别已完成。
- 冻结引擎全库快照实测 2.70 秒；重复建议删除预检 0.82 秒。真实 Swift decoder 验证 6,324 项及事件结构通过。
- 重复共 22 项。进一步核对时间、地点及独有说明后，当前仅 1 项满足自动预选条件；其余保留人工核对。只读预检确认推荐项的精确资产标识通过，未删除任何用户照片。
- 24 条测试通过，新增猫个体身份不臆造、低置信猫不归类、会议关联、远距离不误关联、时间间隔拆分、未知日期不合并、保留副本覆盖独有元数据等。
- 已修复照片区宽度自适应（上一轮实际窗口验证 5/8 列切换）。该轮时间线窗口曾受 `cgWindowNotFound` 阻塞；本轮已恢复并完成上述视觉验收。

## 手动整理与删除验收（此前版本）

运行环境：Apple Silicon，macOS 27；安装产物 `/Applications/PhotoDesk.app`，内置 osxphotos 0.76.1。

- 用 App 菜单创建一张明确标记的合成测试照片，PhotoKit 回读确认导入。未将用户原有照片用于写入或删除测试。
- 实际窗口：全部照片加载、搜索限定一张、放大预览、按月倒序、勾选、删除范围预检、取消后保留、确认删除后刷新。
- 分类相册：通过 App 勾选该测试图的一项年月建议，确认写入，实际相册成员回读一致。
- 标题及敏感标记：内置已安装引擎只选择同一测试图，补标题、OCR 识别测试号码、加入敏感相册、写关键词，均回读一致。
- 失败恢复：测试图被标记敏感后，旧计划试图加入待清理相册，被“照片现在已受保护”拒绝。没有错误报告成功。
- 删除：只删除该测试图；PhotoKit 记录 `deleted_and_verified`。独立 osxphotos 回读同一 UUID：普通库 0 项、最近删除 1 项。未执行永久删除或恢复；恢复入口使用系统“照片”。
- 打包引擎真实只读检查：audit、classify、title、triage、sensitive、duplicates、library、OCR 合成探针和 apply 预检通过；六种计划通过实际 Swift decoder。
- 17 条回归测试通过，包含未同步照片、重复选中去重、共享/失效照片拒绝、Cloud GUID 失效不回退、旧系统配置缺失、部分写入失败。
- 冷启动实际读取图库成功；页面返回使用缓存。修复任务完成后进度轮询再次写入旧状态的问题；实际窗口完成后不再显示忙碌提示。

范围限制：场景标签在本机 macOS 27 不可读；OCR 不覆盖缺少原片的资产。截图票据页优先使用已有待清理相册，未把上述敏感测试图强行加入该相册，故该页本轮只验真实读取和计划解码。重复核对仅按相同原片指纹，不能推断编辑结果相同。系统额外删除弹框依权限和系统版本而定，App 自身确认始终存在。

含图库信息的详细计划、执行回执、测试图片标识保存在 gitignored `qa/` 与用户 Application Support/PhotoDesk；不入仓库。
