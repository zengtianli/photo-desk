# PhotoDesk v1.0.1 build 10 媒体验收

录制日期：2026-09-10。环境：Apple M4、macOS 27.0。主线程用 CUA AX 操作独立合成图库 App；本剪辑任务没有启动应用、操作 UI、装机或重新构建发行包。

| 成片 | 时长 | 实际覆盖 |
|---|---:|---|
| `timeline.mp4` | 14.6 秒 | 全部时间线 → 猫时间线 → 会议与工作 → 全部；末段放大三张真实片段卡片 |
| `review.mp4` | 17.9 秒 | 选生活片段 → 快速预览 `sample-03.png`；明确标注补拍后进入重复核对，放大实际建议 |

原片分别为 22.15、28.08、13.035 秒，无音频。保留片段为原速，剪去等待；11 段精确剪点、裁剪像素、原片 SHA-256 与成片时间映射保存在 `edit-plan.json`、`recording.json`。局部放大及补拍在成片持续标注；Pillow 只绘制产品画面外的青绿色字幕，海报从对应成片抽帧。

受委派的媒体验收者检查了原片关键帧、操作前后，以及最终 11 段各自首/中/尾共 33 帧、两处结果放大、两张海报和两张正式截图。所见均为合成输入与 PhotoDesk 窗口，没有其他应用或个人材料；字幕没有遮挡产品画面。根线程保存的 `timeline.png`、`preview.png` 哈希未变，三段原片未改。

两段成片完整解码通过，H.264 / yuv420p、1280 × 1096、30 fps、无音频，MP4 faststart。产品画面区域没有满足 98% 黑像素且持续至少 0.2 秒的黑段。脚本实测拒绝错误录制 build、原片哈希不符、越界剪点和未标注补拍。

独立复核重新读取真实生产结果与输入清单，见 `result-check.json`：10 项输入与 10 行计划成员集合完全相同，10 项已分析、待处理/不可用/失败均为 0；三个片段各为 3、3、4 项。重复计划有 2 项，其中 1 项预选；两个输入原文件 SHA-256 相同。

原片下一张点击未改变预览，相关无效等待已剪去；review 原片末帧仍为同一预览，没有录到关闭。补拍从时间线重新开始，未伪装成连续操作。个人图库授权、Apple 相册写回、真实照片删除及导航到下一张均未覆盖；成片末段仅展示建议，没有执行删除。

`evidence.json` 固定六项公开媒体及发行 SHA-256，供主页正式构建使用。原片、抽帧检查图、独立 state 与本地开发路径不属于站点公开白名单。
